# Charter

Fill this in. The agent must not silently change the task, dataset, or primary metric.

| Field | Value |
|-------|--------|
| Task | |
| Dataset | |
| Primary metric | |
| Collection | `rec_ctr` |

`Collection` is the experiment-memory name under `EXPMEM_ROOT` (toy DBs: `rec_ctr`, `seq_recall`).
