---
name: patch-reviewer
description: Folded into the Reviewer. Load experiment-reviewer instead. Do not edit code yourself.
---

# 已并入审查者

补丁审查（泄露、change 是否实现、`code_sha256`）由审查者在 run 前完成。读 `.pi/skills/experiment-reviewer/SKILL.md`。结论角色写 `reviewer`（旧文件 `patch-reviewer` 仍被哈希闸门接受）。
